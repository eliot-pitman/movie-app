@movies.map do |movie|
  {
    id: movie.id,
    title: movie.title,
    year: movie.year,
    plot: movie.plot,
    director: movie.director,
    english: movie.english
    # url: movie_url(movie, format: :json)
  }
end
